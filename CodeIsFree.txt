<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CodeIsFree</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
            color: #fff;
            min-height: 100vh;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        header {
            text-align: center;
            padding: 40px 20px;
            position: relative;
        }

        h1 {
            font-size: 4rem;
            background: linear-gradient(45deg, #00ff88, #00d4ff, #ff00ff);
            background-size: 200% 200%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: gradient 3s ease infinite;
            text-shadow: 0 0 30px rgba(0, 255, 136, 0.5);
            letter-spacing: 3px;
        }

        @keyframes gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .subtitle {
            color: #888;
            font-size: 1.2rem;
            margin-top: 10px;
            animation: fadeIn 1s ease-in;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .tabs {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 40px 0;
            flex-wrap: wrap;
        }

        .tab {
            padding: 15px 40px;
            background: #1a1a1a;
            border: 2px solid #333;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            font-size: 1.1rem;
            font-weight: bold;
        }

        .tab::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(0, 255, 136, 0.2);
            transform: translate(-50%, -50%);
            transition: width 0.5s, height 0.5s;
        }

        .tab:hover::before {
            width: 300px;
            height: 300px;
        }

        .tab:hover {
            border-color: #00ff88;
            transform: translateY(-3px);
            box-shadow: 0 5px 20px rgba(0, 255, 136, 0.4);
        }

        .tab.active {
            background: linear-gradient(135deg, #00ff88, #00d4ff);
            color: #000;
            border-color: transparent;
            box-shadow: 0 5px 30px rgba(0, 255, 136, 0.6);
        }

        .tab span {
            position: relative;
            z-index: 1;
        }

        .content {
            display: none;
            animation: slideIn 0.5s ease;
        }

        .content.active {
            display: block;
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .card-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-top: 30px;
        }

        .card {
            background: #1a1a1a;
            border: 2px solid #333;
            border-radius: 20px;
            padding: 30px;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
        }

        .card::after {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent, rgba(0, 255, 136, 0.1), transparent);
            transform: rotate(45deg);
            transition: all 0.5s;
        }

        .card:hover::after {
            animation: shine 1.5s infinite;
        }

        @keyframes shine {
            0% { transform: translateX(-100%) rotate(45deg); }
            100% { transform: translateX(100%) rotate(45deg); }
        }

        .card:hover {
            border-color: #00ff88;
            transform: scale(1.05) translateY(-10px);
            box-shadow: 0 10px 40px rgba(0, 255, 136, 0.3);
        }

        .card-title {
            font-size: 1.5rem;
            margin-bottom: 15px;
            color: #00ff88;
            position: relative;
            z-index: 1;
        }

        .card-status {
            color: #888;
            font-size: 0.9rem;
            margin-bottom: 10px;
            position: relative;
            z-index: 1;
        }

        .card.expanded {
            grid-column: 1 / -1;
        }

        .card-content {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.5s ease;
            position: relative;
            z-index: 1;
        }

        .card.expanded .card-content {
            max-height: 500px;
        }

        .card-description {
            color: #ccc;
            margin: 20px 0;
            line-height: 1.6;
        }

        .code-block {
            background: #0a0a0a;
            border: 1px solid #00ff88;
            border-radius: 10px;
            padding: 20px;
            margin: 20px 0;
            position: relative;
            font-family: 'Courier New', monospace;
            color: #00ff88;
        }

        .copy-btn {
            position: absolute;
            top: 10px;
            right: 10px;
            padding: 8px 15px;
            background: #00ff88;
            color: #000;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            transition: all 0.3s;
        }

        .copy-btn:hover {
            background: #00d4ff;
            transform: scale(1.1);
        }

        .copy-btn.copied {
            background: #ff00ff;
        }

        .link-btn {
            display: inline-block;
            padding: 15px 30px;
            background: linear-gradient(135deg, #00ff88, #00d4ff);
            color: #000;
            text-decoration: none;
            border-radius: 10px;
            font-weight: bold;
            margin-top: 20px;
            transition: all 0.3s;
            box-shadow: 0 5px 20px rgba(0, 255, 136, 0.4);
        }

        .link-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 30px rgba(0, 255, 136, 0.6);
        }

        .status-item {
            background: #1a1a1a;
            border: 2px solid #333;
            border-radius: 15px;
            padding: 25px;
            margin: 20px 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: all 0.3s;
        }

        .status-item:hover {
            border-color: #00ff88;
            transform: translateX(10px);
        }

        .status-label {
            font-size: 1.2rem;
            font-weight: bold;
        }

        .status-indicator {
            width: 20px;
            height: 20px;
            border-radius: 50%;
            animation: pulse 2s infinite;
        }

        .status-online {
            background: #00ff88;
            box-shadow: 0 0 20px #00ff88;
        }

        .status-offline {
            background: #ff0055;
            box-shadow: 0 0 20px #ff0055;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.2); opacity: 0.7; }
        }

        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }

        .particle {
            position: absolute;
            width: 3px;
            height: 3px;
            background: #00ff88;
            border-radius: 50%;
            animation: float 10s infinite;
            opacity: 0.3;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0) translateX(0); }
            25% { transform: translateY(-100px) translateX(50px); }
            50% { transform: translateY(-200px) translateX(-50px); }
            75% { transform: translateY(-300px) translateX(100px); }
        }
    </style>
</head>
<body>
    <div class="particles" id="particles"></div>
    
    <div class="container">
        <header>
            <h1>CodeIsFree</h1>
            <p class="subtitle">Scripts e Executores Premium - Totalmente Gratuito</p>
        </header>

        <div class="tabs">
            <div class="tab active" onclick="showTab('scripts')">
                <span>📜 Scripts</span>
            </div>
            <div class="tab" onclick="showTab('executores')">
                <span>⚡ Executores</span>
            </div>
            <div class="tab" onclick="showTab('status')">
                <span>🔴 Status</span>
            </div>
        </div>

        <div id="scripts" class="content active">
            <div class="card-grid">
                <div class="card" onclick="toggleCard(this)">
                    <h3 class="card-title">🎮 Script de Auto-Farm</h3>
                    <p class="card-status">Clique para ver detalhes e copiar</p>
                    <div class="card-content">
                        <p class="card-description">
                            Este script permite automatizar o farm de recursos em jogos. 
                            Funciona com detecção inteligente de alvos e evita detecção. 
                            Compatível com múltiplas plataformas e jogos.
                        </p>
                        <div class="code-block">
                            <button class="copy-btn" onclick="copyCode(event, this)">Copiar</button>
                            <code>local AutoFarm = true
while AutoFarm do
    game:GetService("Players").LocalPlayer.Character.HumanoidRootPart.CFrame = 
    game:GetService("Workspace").Resources.CFrame
    wait(0.5)
end</code>
                        </div>
                    </div>
                </div>

                <div class="card" onclick="toggleCard(this)">
                    <h3 class="card-title">🚀 Speed Boost Script</h3>
                    <p class="card-status">Clique para ver detalhes e copiar</p>
                    <div class="card-content">
                        <p class="card-description">
                            Aumenta drasticamente a velocidade do seu personagem. 
                            Configurável para evitar kickbacks e com controle de intensidade. 
                            Perfeito para exploração rápida de mapas.
                        </p>
                        <div class="code-block">
                            <button class="copy-btn" onclick="copyCode(event, this)">Copiar</button>
                            <code>local player = game.Players.LocalPlayer
local character = player.Character
character.Humanoid.WalkSpeed = 100
print("Speed boost ativado!")</code>
                        </div>
                    </div>
                </div>

                <div class="card" onclick="toggleCard(this)">
                    <h3 class="card-title">💎 ESP/Wallhack Script</h3>
                    <p class="card-status">Clique para ver detalhes e copiar</p>
                    <div class="card-content">
                        <p class="card-description">
                            Visualize jogadores e itens através das paredes. 
                            Sistema de highlight colorido e rastreamento em tempo real. 
                            Inclui indicador de distância e status dos jogadores.
                        </p>
                        <div class="code-block">
                            <button class="copy-btn" onclick="copyCode(event, this)">Copiar</button>
                            <code>for _, player in pairs(game.Players:GetPlayers()) do
    if player ~= game.Players.LocalPlayer then
        local highlight = Instance.new("Highlight")
        highlight.Parent = player.Character
        highlight.FillColor = Color3.fromRGB(0, 255, 136)
    end
end</code>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div id="executores" class="content">
            <div class="card-grid">
                <div class="card" onclick="toggleCard(this)">
                    <h3 class="card-title">⚡ Executor Pro X</h3>
                    <p class="card-status">Clique para acessar o download</p>
                    <div class="card-content">
                        <p class="card-description">
                            O executor mais poderoso disponível! Suporta todos os scripts modernos, 
                            interface intuitiva e atualizações automáticas. Proteção anti-detecção 
                            integrada e suporte para múltiplos jogos simultaneamente.
                        </p>
                        <a href="https://github.com" class="link-btn" target="_blank" onclick="event.stopPropagation()">
                            🔗 Baixar Executor Pro X
                        </a>
                    </div>
                </div>

                <div class="card" onclick="toggleCard(this)">
                    <h3 class="card-title">🔥 FastExec Ultimate</h3>
                    <p class="card-status">Clique para acessar o download</p>
                    <div class="card-content">
                        <p class="card-description">
                            Executor ultra-rápido com execução instantânea de scripts. 
                            Biblioteca de scripts integrada, modo stealth avançado e 
                            compatibilidade com 99% dos jogos populares.
                        </p>
                        <a href="https://github.com" class="link-btn" target="_blank" onclick="event.stopPropagation()">
                            🔗 Baixar FastExec Ultimate
                        </a>
                    </div>
                </div>

                <div class="card" onclick="toggleCard(this)">
                    <h3 class="card-title">💫 Quantum Executor</h3>
                    <p class="card-status">Clique para acessar o download</p>
                    <div class="card-content">
                        <p class="card-description">
                            Tecnologia de ponta em execução de scripts. Interface personalizável, 
                            sistema de favoritos, hub de scripts da comunidade e suporte premium 
                            24/7. O futuro dos executores está aqui!
                        </p>
                        <a href="https://github.com" class="link-btn" target="_blank" onclick="event.stopPropagation()">
                            🔗 Baixar Quantum Executor
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <div id="status" class="content">
            <h2 style="text-align: center; margin-bottom: 30px; color: #00ff88;">Status dos Serviços</h2>
            
            <div class="status-item">
                <span class="status-label">🌐 Servidor Principal</span>
                <div class="status-indicator status-online"></div>
            </div>

            <div class="status-item">
                <span class="status-label">📦 API de Scripts</span>
                <div class="status-indicator status-online"></div>
            </div>

            <div class="status-item">
                <span class="status-label">⚡ Rede de CDN</span>
                <div class="status-indicator status-online"></div>
            </div>

            <div class="status-item">
                <span class="status-label">🔒 Sistema de Segurança</span>
                <div class="status-indicator status-online"></div>
            </div>

            <div class="status-item">
                <span class="status-label">💬 Suporte ao Usuário</span>
                <div class="status-indicator status-online"></div>
            </div>

            <div class="status-item">
                <span class="status-label">🔄 Atualizações Automáticas</span>
                <div class="status-indicator status-online"></div>
            </div>

            <div style="text-align: center; margin-top: 40px; color: #888;">
                <p>✅ Todos os sistemas operacionais</p>
                <p style="margin-top: 10px;">Última verificação: Agora mesmo</p>
            </div>
        </div>
    </div>

    <script>
        // Criar partículas flutuantes
        const particlesContainer = document.getElementById('particles');
        for (let i = 0; i < 30; i++) {
            const particle = document.createElement('div');
            particle.className = 'particle';
            particle.style.left = Math.random() * 100 + '%';
            particle.style.top = Math.random() * 100 + '%';
            particle.style.animationDelay = Math.random() * 10 + 's';
            particle.style.animationDuration = (Math.random() * 10 + 10) + 's';
            particlesContainer.appendChild(particle);
        }

        function showTab(tabName) {
            // Esconder todos os conteúdos
            const contents = document.querySelectorAll('.content');
            contents.forEach(content => content.classList.remove('active'));

            // Remover classe active de todas as tabs
            const tabs = document.querySelectorAll('.tab');
            tabs.forEach(tab => tab.classList.remove('active'));

            // Mostrar conteúdo selecionado
            document.getElementById(tabName).classList.add('active');

            // Adicionar classe active na tab clicada
            event.target.closest('.tab').classList.add('active');

            // Fechar todos os cards expandidos
            const cards = document.querySelectorAll('.card.expanded');
            cards.forEach(card => card.classList.remove('expanded'));
        }

        function toggleCard(card) {
            const isExpanded = card.classList.contains('expanded');
            
            // Fechar todos os outros cards
            const allCards = document.querySelectorAll('.card');
            allCards.forEach(c => c.classList.remove('expanded'));

            // Toggle no card clicado
            if (!isExpanded) {
                card.classList.add('expanded');
            }
        }

        function copyCode(event, button) {
            event.stopPropagation();
            const codeBlock = button.parentElement;
            const code = codeBlock.querySelector('code').textContent;
            
            navigator.clipboard.writeText(code).then(() => {
                button.textContent = '✓ Copiado!';
                button.classList.add('copied');
                
                setTimeout(() => {
                    button.textContent = 'Copiar';
                    button.classList.remove('copied');
                }, 2000);
            });
        }

        // Efeito de digitação no título
        const title = document.querySelector('h1');
        const originalText = title.textContent;
        title.textContent = '';
        let i = 0;

        function typeWriter() {
            if (i < originalText.length) {
                title.textContent += originalText.charAt(i);
                i++;
                setTimeout(typeWriter, 150);
            }
        }

        typeWriter();
    </script>
</body>
</html>